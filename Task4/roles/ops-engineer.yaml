apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: ops-engineer-role
  namespace: pd
rules:
  - apiGroups: [""]
    resources: ["pods", "services", "events", "configmaps"]
    verbs: ["get", "list", "watch", "delete"]
